# 任务页面坐标 (1280x720 绝对像素)
# 引用格式: mission.<键>

# ── 回退 ──
back: [28, 42]            # 回退按钮 ◁ (任务页 → 首页)

# ── 面板子标签 ──
panel:
  all: [197, 48]          # 全部
  main: [358, 49]         # 主线
  daily: [518, 44]        # 日常
  weekly: [692, 42]       # 周常
  timed: [854, 41]        # 限时
